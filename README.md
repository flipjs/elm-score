# ScoreKeeper example using Elm

Scorekeeper example used in http://courses.knowthen.com with my own implementation using Elm Architecture pattern.

Run `elm-reactor --port=3000`,
then go to http://localhost:3000
